
#spring
spring:
  datasource:
    #???????????
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite::resource:db/libary.db
    username:
    password:

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #mybatis-plus????????????SQL??